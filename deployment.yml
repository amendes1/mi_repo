apiVersion: apps/v1
kind: Deployment
metadata:
  name: mi-app
  namespace: mi-namespace
spec:
  replicas: 3
  selector:
    matchLabels:
      app: mi-app
  template:
    metadata:
      labels:
        app: mi-app
    spec:
      containers:
      - name: nginx
        image: olaff33/lanueva:latest
        ports:
        - containerPort: 80

